sudo: false
language: rust
rust:
  - stable
  - beta
  - nightly
matrix:
  allow_failures:
    - rust: nightly
os:
  - linux
  - osx

compiler:
  - gcc
  - clang

addons:
  apt:
    sources:
      - george-edison55-precise-backports # cmake 3.2.3 / doxygen 1.8.3
      - ubuntu-toolchain-r-test # gcc-4.8 / g++-4.8
    packages:
      - cmake
      - cmake-data
      - g++
      - gcc-4.8
      - g++-4.8
      - ragel
      - libbz2-dev
      - libpcap-dev
      - python-dev
      - tree

cache:
  directories:
    - $HOME/boost
    - $HOME/hyperscan

before_install:
  - .travis/setup-travis.sh
  - export BOOST_ROOT=$HOME/boost
  - export HYPERSCAN_ROOT=$HOME/hyperscan

before_script:
  - .travis/build-hyperscan.sh

after_failure:
  - tree -h
env:
  global:
    - RUST_BACKTRACE=1